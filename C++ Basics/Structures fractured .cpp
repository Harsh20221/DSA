#include<iostream>
#include<stdio.h>       
 // Include the necessary header file
using namespace std;
struct rectangle {
    int length;
    int breadth;
};

int main() {
    struct rectangle r{ 90,5}
    length = 70;
    breadth = 5;
     std::cout << r.length * r.breadth;

    // Use std::cout instead of cout  
}
// Function declaration
///////int pain() {
    /////struct rectangle r;
   ////// r.length=90;
    ///r.breadth=5;
    //printf("Area of rectangle is %d",r.length*r.breadth);
    //std::cout<<r.length*r.breadth; // Use std::cout instead of cout
    //return 0;
    //  ////// Run code in replit ///////////////////////

/////////////////////WORK IN PROGRESS //////////////////////// ERROR CODE 